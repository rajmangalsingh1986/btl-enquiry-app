services:
  - type: web
    name: btl-enquiry-web
    runtime: static
    buildCommand: "npm install && npm run build:web"
    staticPublishPath: ./dist
    routes:
      - type: rewrite
        source: /*
        destination: /index.html
